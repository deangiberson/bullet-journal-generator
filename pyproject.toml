[project]
name = "bullet-journal-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "behave>=1.2.6",
]
